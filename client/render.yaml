services:
  - type: web
    name: user-management-client
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist/client
    envVars:
      - key: NODE_VERSION
        value: 18